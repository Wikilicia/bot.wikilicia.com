{
  "scripts": {
    "docs:dev":    "vitepress dev",
    "docs:build":  "vitepress build",
    "docs:preview":"vitepress preview",
    "deploy":      "npm run docs:build && gh-pages -d .vitepress/dist"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vue":       "^3.5.13",
    "gh-pages":  "^5.0.0"
  }
}
